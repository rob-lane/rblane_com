<ul>
  <% @settings.each do |setting| %>
    <li>
      <%= form_for setting, :url => admin_settings_path(setting), :method => :put do |form| %>
        <strong><%= setting.name %></strong>:<%= form.text_field :value %>
        <%= form.submit 'Save' %>
      <% end %>
    </li>
  <% end %>
</ul>
